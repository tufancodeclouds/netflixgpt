const PageLoader = () => {
  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80">
      <div className="netflix-ring"></div>
    </div>
  );
};

export default PageLoader;
